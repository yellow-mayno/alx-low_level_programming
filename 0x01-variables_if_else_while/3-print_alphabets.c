#include <stdio.h>

/**
 * main - sdfsqdfsqdf
 *
 * Return: 0 dsfsdfsdf
*/
int main(void)
{
	int a;
	char ch;

	for (a = 0x61 ; a < 0x7b  ; a++)
	{
		ch = (char)a;
		putchar(ch);
	}
	for (a = "A" ; a <= "Z" ; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
