#include <stdio.h>

/**
 * main - dsjfshdkjfsh
 *
 * Return: 0 4654444545
 */
int main(void)
{
	int a;

	for (a = 0x61; a < 0x7b; a++)
	{
		if (a != 0x65 && a != 0x71)
			putchar(a);
	}
	putchar(10);
	return (0);
}
