#include <stdio.h>

/**
 * main - sdfsqdfsqdf
 *
 * Return: 0 dsfsdfsdf
*/
int main(void)
{
	int a;
	char ch;

	for (a = 0x7a ; a >= 0x61  ; a--)
	{
		ch = (char)a;
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
