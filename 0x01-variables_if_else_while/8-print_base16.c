#include <stdio.h>

/**
 * main - dsfsdfsdsf
 *
 * Return: 0 dsfsdfsdsfsdfsd;df
 */
int main(void)
{
	int i;

	for (i = 0x30; i <= 0x46; i++)
	{
		if (i < 0x3A || i > 0x40)
			putchar(i);
	}
	putchar(0xA);
	return (0);
}
