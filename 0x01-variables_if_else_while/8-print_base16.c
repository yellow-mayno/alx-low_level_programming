#include <stdio.h>

/**
 * main - dsfsdfsdsf
 *
 * Return: 0 dsfsdfsdsfsdfsd;df
 */
int main(void)
{
	int i;

	for (i = 0x30; i <= 0x66; i++)
	{
		if (i < 0x3A || i > 0x60)
			putchar(i);
	}
	putchar(0xA);
	return (0);
}
