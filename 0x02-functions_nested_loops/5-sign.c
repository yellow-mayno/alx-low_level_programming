#include "main.h"
/**
 * print_sign - blablabla
 * @n :blobloblo
 *
 * Return: 1orOooosodqosdoqso
 */
int print_sign(int n)
{
	if (n > 0)
		return (1);
	else if (n < 0)
		return (-1);
	else
		return (0);
}
