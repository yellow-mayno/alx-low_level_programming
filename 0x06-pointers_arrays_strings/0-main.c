#include "main.h"
#include <stdio.h>
/**
* main - dsfsd
* Return: dfdf
*/
int main(void)
{
	char dest[] = "It's so";
	char src[] = " fkn over !\n";

	printf("%s\n", dest);
	_strcat(dest, src);
	printf("%s\n", dest);
	return (0);
}
