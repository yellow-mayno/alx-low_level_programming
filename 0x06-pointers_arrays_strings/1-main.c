#include <stdio.h>
#include "main.h"
/**
* main - dsfsdf
* Return: 0dsfdsf
*/
int main(void)
{
	char dest[] = "it'so fkn over ";
	char src[] = "over over over over over !\n";
	int n = 2;

	_strncat(dest, src, 5 * n);
	printf("%s\n", dest);
	return (0);
}
