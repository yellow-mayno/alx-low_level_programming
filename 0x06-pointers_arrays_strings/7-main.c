#include <stdio.h>
#include "main.h"
/**
* main -sdfsd
* Return: dfd
*/
int main(void)
{
	char text[] = "zemmour eonssori dzab";
	char *ptr;

	ptr = leet(text);
	printf("%s\n", text);
	printf("%s\n", ptr);
	return (0);
}
