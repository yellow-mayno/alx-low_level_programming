#include <stdio.h>
#include "main.h"

int factorial(int n)
{
	int neg = -10;
	int nada = 0;
	int pos = 5;

	printf("%d , %d, %d\n", factorial(neg), factorial(nada), 
																																																																																			factorial(pos));
	return (0);
}
